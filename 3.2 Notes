
public class Main
{
	public static void main(String[] args) 
	{
		System.out.println("_Structure of an if-statement_");
		if(boolean condition)
		    action;
		    //Or 
		if(boolean condition) //ALSO use brackets when multiple actions
		{
		    action;
		}
		
		System.out.println("Example. Rolling two standard six sided dice. ");
		int d1 = (int)(Math.random()*6) + 1;
        int d2 = (int)(Math.random()*6) + 1;
        int sum = d1 + d2;
        if(d1 == d2)
            sum *= 2;
        if(d1 == 1)
            sum = 0;
        if(d2 == 1)
            sum = 0;  
        //no brackets for these if statements because order doesn't matter
        
        System.out.println("You can, but not required to, have an else statment with it.");
        System.out.println("We NEVER have both or neither running t");
        System.out.println("When you have an if-else statement, you are always guaranteed an outcome.");
	}
}
